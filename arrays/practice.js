//let numbers =[1,2,3,4];
//let squares =[];
////let squares = numbers.forEach(num => squares.push(num*num));
// squares

let numbers = [1, 2, 3, 4]
let squares = [];
//numbers.forEach(num => squares.push(num * num));
//numbers.forEach(num => squares.push(num * num));
//squares.forEach (num => console.log(num));
//squares = numbers.map (num => (num*num));
//squares = numbers.map (num => (num*num));

//for in loop goes through every elements in an array

/*let numbersNew = numbers.filter(num => num >=2);

for ( x in numbersNew){

  console.log(numbersNew[x]);
}
let numbersReduce;
numbersReduce = numbers.reduce((accumulater,element) => accumulater*element, 1);
console.log(numbersReduce);

let strings = ['h','e','l','l','o'];
let newString = strings.reduce((accumulater,element)=> accumulater+element.toLocaleUpperCase(),'M-');
console.log(newString);*/

let a = [,,];
console.log(a.length);
console.log((Object.keys(a).length));






  



